FROM node:20-alpine

WORKDIR /app

COPY package*.json ./

# Keep install layer cacheable.
RUN npm ci --legacy-peer-deps

COPY src ./src
COPY scripts ./scripts

ENV NODE_ENV=production
ENV PORT=5000

EXPOSE 5000

CMD ["node", "src/server.js"]
